{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    //Server=SQL Server Name
    //Database=DAtabase name
    //MultipleActiveResultSets= Allowing multiple sql queries at once
    //Trusted_Connection= Authenticating with local authentication (Windows authentication) if it's hosted on the network then you have to use user:pw
    //TrustServerCertificate= Enable the SSL certificate to talk to the database
    "ConnectString": "Server=Dual;Database=PUSGS;MultipleActiveResultSets=True;Trusted_Connection=True;TrustServerCertificate=Yes;" //Connect string
  },
  "secret": "secret_1234"
}
